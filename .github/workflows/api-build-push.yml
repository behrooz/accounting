name: Build and Push API

on:
  push:
    branches:
      - main
    paths:
      - "api/**"
      - ".github/workflows/api-build-push.yml"

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          config-inline: |
            [registry."188.132.232.168:30836"]
              http = true
              insecure = true

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./api
          file: ./api/Dockerfile
          push: true
          tags: |
            188.132.232.168:30836/accounting-api:${{ github.run_number }}
            188.132.232.168:30836/accounting-api:latest
          cache-from: type=registry,ref=188.132.232.168:30836/accounting-api:latest
          cache-to: type=inline

      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: v4.0.4

      - name: Configure kubectl
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBECONFIG }}" | base64 -d > $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      - name: Helm upgrade
        run: |
          NAMESPACE="${K8S_NAMESPACE:-default}"
          IMAGE_TAG="${{ github.run_number }}"

          echo "Upgrading accounting-api with image tag ${IMAGE_TAG}"

          # Do not use --wait: pods in this cluster never get Ready=True
          # (vCluster status sync), so Helm would always time out.
          helm upgrade --install accounting-api ./api/chart \
            --namespace "${NAMESPACE}" \
            --create-namespace \
            --set image.tag="${IMAGE_TAG}" \
            --server-side=false

          echo "Waiting for pod to be Running..."
          kubectl -n "${NAMESPACE}" rollout status deployment/accounting-api --timeout=3m || true
          kubectl -n "${NAMESPACE}" get pods -l app.kubernetes.io/name=accounting-api -o wide

          echo "✅ API upgrade submitted (image tag: ${IMAGE_TAG})"
        env:
          K8S_NAMESPACE: ${{ secrets.API_K8S_NAMESPACE || secrets.K8S_NAMESPACE || 'default' }}
