apiVersion: v1
kind: Service
metadata:
  name: {{ include "accounting-front.fullname" . }}
  labels:
    {{- include "accounting-front.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  {{- if .Values.service.publishNotReadyAddresses }}
  publishNotReadyAddresses: true
  {{- end }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "accounting-front.selectorLabels" . | nindent 4 }}
