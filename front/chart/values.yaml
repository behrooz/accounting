# Default values for accounting-front
replicaCount: 1

# maxSurge: 0 = don't create a second pod; terminates old first (for small nodes)
strategy:
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 0
    maxUnavailable: 1

image:
  repository: 188.132.232.168:30836/accounting-front
  pullPolicy: IfNotPresent
  tag: "40"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1001

service:
  type: ClusterIP
  port: 3000
  targetPort: 3000
  annotations: {}
  # Needed when pod Ready condition never becomes True (e.g. vCluster status sync)
  publishNotReadyAddresses: true

# Prometheus Metrics Configuration
metrics:
  enabled: false
  serviceMonitor:
    enabled: true
    interval: 30s
    scrapeTimeout: 10s
    labels: {}
    annotations: {}
    relabelings: []
    metricRelabelings: []

ingress:
  enabled: true
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
  hosts:
    - host: ns-xp45-default-accounting-front.bugx.ir
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: accounting-front-tls
      hosts:
        - ns-xp45-default-accounting-front.bugx.ir

resources:
  limits:
    cpu: "1"
    memory: 2Gi
  requests:
    cpu: 250m
    memory: 512Mi

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

# Environment Variables
# NOTE: NEXT_PUBLIC_API_BASE_URL is baked into the image at docker build time.
# Rebuild the front image with --build-arg when changing the public API URL.
env:
  - name: NODE_ENV
    value: "production"
  - name: PORT
    value: "3000"
  - name: HOSTNAME
    value: "0.0.0.0"
  - name: NEXT_TELEMETRY_DISABLED
    value: "1"
  # Server-side auth checks (product assistant). Use in-cluster API URL.
  - name: API_BASE_URL
    value: "http://accounting-api:8080/api"

# Cursor SDK product assistant. The API key must be provided by an existing
# Kubernetes Secret; it is never stored in chart values or exposed publicly.
cursor:
  enabled: true
  secretName: accounting-front-cursor
  secretKey: CURSOR_API_KEY

# ConfigMap for additional configuration
configMap:
  enabled: false
  data: {}

# Secrets for sensitive data (optional)
secrets:
  enabled: false
  data: {}

# Probes (disabled — set enabled: true to turn on)
probes:
  enabled: false

livenessProbe:
  httpGet:
    path: /
    port: http
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3

readinessProbe:
  httpGet:
    path: /
    port: http
  initialDelaySeconds: 10
  periodSeconds: 5
  timeoutSeconds: 3
  failureThreshold: 3

# Additional volumes
volumes: []

volumeMounts: []

# Init containers
initContainers: []

# Sidecar containers
sidecars: []

# Pod Disruption Budget
podDisruptionBudget:
  enabled: false
  minAvailable: 1
  # maxUnavailable: 1

# Network Policy
networkPolicy:
  enabled: false
  policyTypes:
    - Ingress
    - Egress
  ingress: []
  egress: []
